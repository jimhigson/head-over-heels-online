# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-editor
dist-tauri
dist-ssr
*.local

.DS_Store

#vite-pwa dist for in dev
dev-dist

.vite

# local miniflare state from wrangler r2/dev
.wrangler

supabase/.temp

# Playwright
playwright-report/
test-results/

# transient dir used by the oxlint pre-commit lint-fix wrapper
.oxlint-fix-tmp.*

build-stats*.html

src-tauri/gen
src-tauri/target
.gitnexus

# python bytecode caches (eg the guided-review skill scripts)
__pycache__/
*.pyc

# python toolchain for scripts/genFont.ts (pnpm gen:font)
scripts/font/.venv

# local scratch space for one-off probes and screenshots
.scratch

# pnpm's content-addressable store, when it lands inside the worktree
.pnpm-store/
