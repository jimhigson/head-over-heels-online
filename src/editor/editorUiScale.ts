/**
 * the on-screen scale the editor's ui renders at, in retro (upscaled) pixels.
 * Applied to the html ui through the `.scale-editor` css class (`--scale`) and
 * to the pixi annotations by the annotations renderer, so both stay the same
 * apparent size on screen.
 */
export const editorUiScale = 2;
