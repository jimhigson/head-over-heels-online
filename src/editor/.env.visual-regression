# editor build for the playwright editor suite (playwright.editor.config.ts):
# production-shaped, but with the redux store and the editor's e2e api exposed
# on the window (see `import.meta.env.MODE === "visual-regression"`)
VITE_APP=editor
VITE_GAME_URL=/
