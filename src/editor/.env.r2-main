# editor build for R2 hosting (served from ed.blockstack.dev), loaded by
# `vite build --config vite.editor.config.ts --mode r2-main`. Links back to the
# game on its own origin.
VITE_APP=editor
VITE_GAME_URL=https://blockstack.dev/
