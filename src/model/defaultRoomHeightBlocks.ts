/**
 * the height (in blocks) of a room that doesn't set its own `height`. Only a few
 * original rooms override it, to tune where the room above loads during a jump.
 */
export const defaultRoomHeightBlocks = 10;
