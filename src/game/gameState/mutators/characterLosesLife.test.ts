import { test } from "vitest";

test("player on their own in a room loses a life (but still has some left)", () => {});
test("player on their own in a room loses their last life", () => {});
test("when both characters enter the same room separately, and the first to enter dies, the other stays at the same place in the same room when they re-join", () => {});
test("when both characters enter the same room separately, and the second to enter dies, the other stays at the same place in the same room when they re-join", () => {});
test("when head-heels (joined) enter a room, separate, and one dies, they re-enter joined", () => {});
test("when head-heels (joined) enter a room, and die still joined", () => {});
