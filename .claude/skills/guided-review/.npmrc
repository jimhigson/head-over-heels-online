# react-resizable-panels asks for react as a peer, and preact/compat is the
# react it gets - installing the real one would put a second renderer in
# node_modules for a bundler alias to have to route around
auto-install-peers=false
