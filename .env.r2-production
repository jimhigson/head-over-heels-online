# game build for R2 hosting (served from blockstack.ing), loaded by
# `vite build --mode r2-production`. The editor lives on a separate origin here.
VITE_APP=game
VITE_EDITOR_URL=https://ed.blockstack.ing/
